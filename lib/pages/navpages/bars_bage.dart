import 'package:flutter/cupertino.dart';

class BarsPage extends StatelessWidget {
  const BarsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("welcome to Bars Page"),
      ),
    );
  }
}
